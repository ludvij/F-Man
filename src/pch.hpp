#pragma once

// precompiled header - F-Man

// core
#include <filesystem> // IWYU pragma: keep
#include <fstream>    // IWYU pragma: keep
#include <ios>        // IWYU pragma: keep
#include <iostream>   // IWYU pragma: keep
#include <streambuf>  // IWYU pragma: keep

// data structures
#include <deque>         // IWYU pragma: keep
#include <memory>        // IWYU pragma: keep
#include <unordered_map> // IWYU pragma: keep
#include <variant>       // IWYU pragma: keep
#include <vector>        // IWYU pragma: keep

// algorithms
#include <algorithm> // IWYU pragma: keep
#include <ranges>    // IWYU pragma: keep

// string types
#include <cstring>     // IWYU pragma: keep
#include <string>      // IWYU pragma: keep
#include <string_view> // IWYU pragma: keep

// int definitions
#include <cstddef> // IWYU pragma: keep
#include <cstdint> // IWYU pragma: keep

// utility
#include <bit>          // IWYU pragma: keep
#include <compare>      // IWYU pragma: keep
#include <print>        // IWYU pragma: keep
#include <stdexcept>    // IWYU pragma: keep
#include <system_error> // IWYU pragma: keep
#include <utility>      // IWYU pragma: keep

// algorithms
#include <algorithm> // IWYU pragma: keep
#include <numeric>   // IWYU pragma: keep
#include <ranges>    // IWYU pragma: keep

#include <ludutils/lud_assert.hpp>     // IWYU pragma: keep
#include <ludutils/lud_mem_stream.hpp> // IWYU pragma: keep
#include <ludutils/lud_parse.hpp>      // IWYU pragma: keep
